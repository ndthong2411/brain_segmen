# nnU-Net Model Configuration
# Auto-loaded when --model nnunet is specified

model:
  model_type: "nnunet"
  in_channels: 4
  num_classes_seg: 4
  num_classes_cls: 2

  # nnU-Net specific
  base: 32
  deep_supervision: true
